---
name: example-api
title: Example HTTP API — reference
summary: Illustrates the doc-entry convention; agents consume frontmatter for discovery and body on hydration.
topics: [example, http-api, reference]
---

# Example API

This is a placeholder document used to demonstrate the `docs/` entry convention.

## Endpoints

- `GET /things` — list things
- `POST /things` — create a thing
- `DELETE /things/:id` — delete a thing

## Auth

Bearer token in `Authorization` header.

Delete this file when the repository has real documentation.
